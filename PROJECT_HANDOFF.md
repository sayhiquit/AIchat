# AI人际管家项目交接与换机继续开发指南

更新时间：2026-07-29  
当前桌面版：`0.2.0`  
代码仓库：[sayhiquit/AIchat](https://github.com/sayhiquit/AIchat)  
当前主分支：`main`  
功能基线提交：`36a5666`（全局 UI 优化完成）

这份文档用于换电脑后恢复开发环境、迁移真实使用数据，并让后续开发者或 AI 能快速理解项目现状与设计思路。

> 重要：GitHub 是项目源码、文档和开发历史的唯一主来源，但它不会保存真实人物、聊天记录、API Key、Electron 本地缓存、`release/` 安装包或 `node_modules/`。换电脑前必须另外完成加密记忆导出；如需完整保留 Key 和所有本机偏好，还要关闭软件后备份 `%APPDATA%\ai-relationship-butler`。

## 0. 接手者 30 秒摘要

- 从 `https://github.com/sayhiquit/AIchat.git` 克隆，继续使用 `main` 分支。
- 当前版本是 Windows Electron 桌面版 `0.2.0`，软件版是真实产品主线，不再单独维护网页版。
- 核心代码集中在 `app.js`，UI 在 `index.html` 和 `styles.css`，桌面入口在 `electron/`。
- 当前已经完成人际网、人物画像、模拟对话、真实记忆、聊天导入预览、AI 蒸馏、日程表、AI 规划台、模型配置和本地数据迁移。
- 当前最该做的是持续真实使用、修稳定性、加强微信/QQ/钉钉/飞书导入，然后做后端模型代理；账号云同步和自动更新排在后面。
- 模拟对话默认不是真实记录，只有明确保存的“新增记忆”才参与画像。
- AI 规划必须经过“多个方向 → 用户选择 → 详细排期 → 用户确认 → 正式日程”。
- 公开上线前禁止继续在前端暴露共享模型 Key，必须增加后端代理、限流和成本控制。
- 每次桌面功能更新都要更新三处版本号、运行验证、打包、检查新版程序、整理 release、提交并推送 GitHub。
- 任何真实人物、聊天、Key 和本地缓存都不能提交到仓库或打包给其他用户。

新电脑只恢复开发代码时，最短路径：

```powershell
git clone https://github.com/sayhiquit/AIchat.git
Set-Location AIchat
npm.cmd ci
npm.cmd run validate
npm.cmd run desktop
```

开始任何新改动前先执行：

```powershell
git pull --ff-only origin main
git status --short --branch
```

## 1. 项目目前是什么

AI人际管家是一款本地优先的 Windows 桌面助手。它不是一次性聊天机器人，而是持续维护人物背景、真实互动、沟通风格、关系重要程度和日程计划的个人助理。

当前产品主线只有桌面软件版。`index.html`、`styles.css`、`app.js` 看起来像网页文件，但它们由 Electron 加载，后续开发、验证、打包和分发都应以桌面软件为准，不再单独维护网页版。

核心原则：

- AI 帮助用户理解关系、准备行动和表达清楚，不鼓励操控他人。
- 心理分析必须保留不确定性，使用“可能、倾向、建议”等表达。
- 人物画像既要看说话风格，也要结合现实背景、权力关系、亲近程度和联系频率。
- 普通模拟对话不进入真实历史；只有用户明确点击“新增记忆”并保存，才参与后续画像蒸馏。
- AI 计划先给多个方向，用户确认后才生成详细排期，再次确认后才写入正式日程。
- 本地真实人物、聊天记录和 API Key 永远不能打进分发安装包或提交到 GitHub。

## 2. 当前完成进度

| 模块 | 状态 | 当前能力 |
| --- | --- | --- |
| 人际关系地图 | 可用 | 固定关系脉络、分类聚焦、分支展开/收缩、人物聚焦、详情展开、返回动画 |
| 分类与人物管理 | 可用 | 分类和人物增删改查、搜索、重要程度筛选、排序、默认模板、清空预设 |
| 人物画像 | 可用 | 人物背景、关系分量、亲近程度、联系频率、性格、注意事项、关系策略、记忆管理 |
| 模拟对话 | 可用 | 对方开场、我方开场、A/B/C 推荐、自定义回复、AI 预测对方、修改真实回复、输入动画 |
| 画像蒸馏 | 基础可用 | 手动文本、聊天文件预览、确认后蒸馏、稳定模式与单次情绪区分 |
| 聊天导入 | 基础可用 | TXT、CSV、JSON、Markdown、HTML；BAK/BAT 只做尝试性读取 |
| 日程时间表 | 可用 | 日期、时间、人物绑定、完成、延期、忽略、删除、月历任务数量 |
| AI 规划台 | 可用 | 需求理解、三个方案、阶段与每日任务、协作对象、资源、一键写入日程 |
| 模型设置 | 可用 | DeepSeek 预设、OpenAI 兼容接口、自定义 Base URL、模型、API Key、连接测试 |
| 数据迁移 | 可用 | 普通 JSON、密码加密 JSON、导入恢复、本地存储诊断 |
| 桌面工程 | 可用 | Electron 运行、Windows NSIS 安装包、最小窗口、响应式布局、自动更新入口 |
| 云账号与同步 | 未开始 | 需要后端、登录、加密同步和冲突处理 |
| 自动更新服务 | 仅入口 | Electron 已有更新逻辑，但还没有线上更新源 |
| 聊天软件悬浮助手 | 后期规划 | 等服务器和隐私方案完成后再做 |
| 手机端与声纹分析 | 长期规划 | 当前不做，正式上线服务器时需要重新提醒评估 |

当前判断：

- 本地 MVP 完成度约 `85%`。
- 自己长期真实使用完成度约 `69%`。
- 小范围分发完成度约 `61%`。
- 面向公众正式上线完成度约 `32%`。

软件已经适合自己使用和小范围测试，但尚不适合直接公开上线。

## 3. 最近完成的重要更新

### 0.2.0 UI 统一

- 将偏米黄色界面调整为更清爽的中性灰绿背景，保留深绿品牌感。
- 统一主次按钮、输入框、聚焦状态、卡片、滚动条和夜间模式。
- 关系管理首屏优先显示搜索筛选、分类设置和人物卡片。
- 对话右侧取消横向滚动，按钮适应窄面板。
- 日程规划台加入电商项目、跨城返乡、能力提升快捷需求。
- 常规窗口和最小窗口下均验证无页面级横向溢出。

### 0.1.9 AI 日程规划

- 需求先生成三个不同方向，用户确认后再生成详细排期。
- 任务包含日期、阶段、执行细节、沟通对象和所需资源。
- 同一个计划不能重复同步到正式日程。
- AI 输出过少、日期缺失或 ID 重复时会自动修复并补全每日计划。
- 离线模拟覆盖电商、跨城出行、学习成长和通用项目。

### 0.1.8 及以前

- 完成人际网、人物详情、聊天式模拟、真实记忆分离、文件导入预览、模型配置、加密备份、诊断模块和基础日程。
- 增加损坏本地缓存的自动备份保护。
- 建立版本一致性、重复 ID 恢复和敏感 Key 检查。

## 4. 代码结构

```text
AIchat/
├─ index.html              Electron 加载的全部静态界面结构
├─ styles.css              全局 UI、动画、夜间模式和响应式布局
├─ app.js                  状态、业务逻辑、渲染、AI 调用、蒸馏、导入导出、日程
├─ electron/
│  ├─ main.js              创建窗口、菜单、更新入口
│  └─ preload.js           安全暴露桌面更新状态桥接
├─ scripts/
│  └─ validate.js          发布前校验入口
├─ package.json            依赖、版本和 electron-builder 配置
├─ package-lock.json       锁定依赖版本
├─ README.md               项目简介和快速运行
├─ DESKTOP_APP.md          桌面运行、打包、数据和更新说明
├─ PROJECT_PROGRESS.md     当前功能进度
├─ TEST_CHECKLIST.md       人工回归清单
└─ PROJECT_HANDOFF.md      本交接文档
```

目前没有前后端拆分、数据库或框架构建流程。核心代码集中在 `app.js`，改动前应先搜索现有函数和状态字段，继续沿用当前模式，避免再造一套并行逻辑。

## 5. 数据存储与隐私

### 5.1 实际存储方式

当前状态存储在 Electron 页面 `localStorage` 中，键名是：

```text
ai_relationship_butler_state_v1
```

Windows 实际 Electron 用户目录通常是：

```text
%APPDATA%\ai-relationship-butler
```

也就是：

```text
C:\Users\你的用户名\AppData\Roaming\ai-relationship-butler
```

`0.2.0` 设置页的存储位置文字可能仍显示 `%APPDATA%\AI人际管家`，这是旧文案；当前 Electron 实际目录以 `%APPDATA%\ai-relationship-butler` 为准。

如果以后修改 `package.json` 的应用名称或 Electron 用户目录配置，路径可能变化。换机时优先使用软件内导出，不要只依赖手动找目录。

### 5.2 软件内导出包含什么

“设置中心 → 记忆迁移 → 导出记忆/加密导出”目前包含：

- 个人资料。
- 分类与人物。
- 人物画像、人物记忆和人物真实聊天历史。
- 学习点。
- 手动日程。
- AI 规划台当前需求、方案和排期草稿。
- 离线模式状态和最近备份时间。

软件内导出当前不包含：

- AI API Key、模型配置和 Base URL。
- 日间/夜间主题等纯界面偏好。
- 当前打开的页面、筛选条件和未保存的模拟过程。

这是有意的安全边界，防止备份文件意外泄漏 Key。

## 6. 换电脑前必须做的事

### 6.1 推荐方式：加密导出

1. 在旧电脑打开 AI人际管家。
2. 进入“设置 → 记忆迁移”。
3. 点击“加密导出”，设置一个不会忘记的密码。
4. 把导出的加密 JSON 保存到可信 U 盘或加密网盘。
5. 密码和备份文件分开保存。
6. 单独记录 DeepSeek/API Key、Base URL 和模型名，不要写进仓库或普通聊天记录。
7. 确认 GitHub 上已经有最新代码：

```powershell
git status --short --branch
git log -3 --oneline
git push origin main
```

8. 如需直接在新电脑安装当前版本，另行复制：

```text
release\AI人际管家-Setup-0.2.0.exe
```

`release/` 被 Git 忽略，克隆仓库后不会自动得到安装包，可以在新电脑重新构建。

### 6.2 完整镜像方式：复制整个用户目录

只有希望连 API Key、主题和全部本机状态一起迁移时，才使用此方式：

1. 完全退出 AI人际管家，确认任务管理器中没有该程序。
2. 按 `Win + R`，输入 `%APPDATA%`。
3. 找到 `ai-relationship-butler` 文件夹。
4. 将整个文件夹压缩并加密保存。
5. 新电脑安装并退出软件后，把该目录恢复到新电脑的 `%APPDATA%`。

这个目录可能包含 API Key、真实人物和聊天记录，严禁提交 GitHub、发给别人或放入公开网盘。

推荐同时保留“加密导出 JSON”和“完整用户目录”两份备份，前者便于跨版本导入，后者用于极端情况下完整恢复。

## 7. 新电脑恢复步骤

### 7.1 安装基础工具

安装：

- Git for Windows。
- Node.js 当前 LTS 版本和 npm。
- 一个代码编辑器，可选 VS Code 或 Codex 桌面端。

验证：

```powershell
git --version
node --version
npm --version
```

### 7.2 克隆代码并运行

```powershell
git clone https://github.com/sayhiquit/AIchat.git
Set-Location AIchat
npm.cmd ci
npm.cmd run validate
npm.cmd run desktop
```

项目不依赖原来的 `E:\回复助手` 路径，放到新电脑任意正常目录即可。不要复制旧电脑的 `node_modules/`，在新电脑重新执行 `npm.cmd ci`。

### 7.3 恢复真实数据

推荐流程：

1. 启动新电脑上的软件。
2. 进入“设置 → 记忆迁移 → 导入记忆”。
3. 选择旧电脑导出的 JSON。
4. 如果是加密导出，输入原密码。
5. 检查人物数、分类数、记忆数和日程数。
6. 进入模型设置，重新填写 DeepSeek/API Key、Base URL 和模型名。
7. 点击“测试连接”。
8. 关闭并重开软件，确认数据仍在。

如果使用完整用户目录恢复，应先退出软件再复制目录，复制完成后重新启动并检查设置页诊断。

## 8. AI 调用思路

当前使用 OpenAI 兼容的 `chat/completions` 接口，DeepSeek 作为默认预设。Key 由用户在设置页填写，并保存在本机状态中。

主要 AI 场景：

- 对话回复：输出对方意图分析和 A/B/C 三种不同策略。
- 我方开场：先模拟对方可能回复，再生成下一轮建议。
- 策略咨询：判断用户是在问“我该怎么做”，还是在优化准备发送的话。
- 人物蒸馏：从真实对话提取稳定沟通模式、做事风格、好感触发和避雷点。
- AI 规划：先生成三个方向，选定后再生成阶段、每日任务、沟通对象和资源。

在线请求失败时应保留本地兜底和明确错误提示，不能让界面无响应。公开上线前必须把模型调用迁移到后端代理，避免 API Key 暴露在前端，并增加用户限流、成本控制和审计。

## 9. 关键业务规则

### 对话与记忆

- 左侧聊天区可以用于模拟，不等于真实历史。
- 刷新后删除未保存模拟，保留已经明确保存的真实记录。
- “新增记忆”后允许编辑；只有点击保存才写入人物记忆并参与蒸馏。
- 离开页面前如果存在未保存记忆草稿，需要提示用户。

### 人物画像

- 人物分类只是组织方式，不能替代人物背景。
- AI 判断必须同时考虑关系、重要程度、权力层级、亲近程度、联系频率和真实反馈。
- 不把单次情绪当成稳定人格，不把模型猜测写成事实。

### 日程规划

- 第一次 AI 调用只给不同方向，避免替用户做决定。
- 用户确认方向后再生成详细排期，节省模型成本。
- 正式写入日程前必须预览确认。
- AI 返回日期缺失、任务过少或重复 ID 时需要自动修复。
- 出行班次、票价、政策、天气等实时信息只能安排“查询核验”任务，不能编造。

## 10. 开发与发布流程

### 10.1 普通开发

```powershell
git status --short --branch
npm.cmd ci
npm.cmd run validate
npm.cmd run desktop
```

修改原则：

- 先阅读现有实现，再沿用当前状态和渲染模式。
- 不覆盖或撤销来源不明的本地修改。
- 不提交真实人物、聊天记录、API Key、`node_modules/` 和 `release/`。
- 页面修改需检查日间、夜间、常规窗口和最小窗口。
- 对话、导入、存储和跨模块状态改动需要补充校验或回归清单。

### 10.2 软件版本发布

每次有实际桌面功能或界面更新时，同步修改：

- `package.json` 的版本。
- `package-lock.json` 根版本和 `packages[""]` 版本。
- `app.js` 的 `appVersion()`。
- 如果前端资源缓存可能影响更新，修改 `index.html` 中 CSS/JS 的查询版本标识。

然后执行：

```powershell
npm.cmd run validate
npm.cmd run dist:win
```

打包后应启动 `release/*-win-unpacked/AI人际管家.exe` 做一次隔离数据验证，再整理 `release/`：

- 最新版本保留 `.exe`、`.blockmap`、`latest.yml` 和 unpacked 目录。
- 历史版本只保留 `.exe`。
- 删除旧 blockmap、旧 unpacked 目录和 `builder-debug.yml`。

最后：

```powershell
git add <本次修改文件>
git commit -m "简明英文提交说明"
git push origin main
```

如果 GitHub 或构建下载需要本机代理，旧电脑曾使用：

```powershell
$env:HTTP_PROXY='http://127.0.0.1:7897'
$env:HTTPS_PROXY='http://127.0.0.1:7897'
```

新电脑代理端口不一定相同，应按新电脑实际设置修改。

## 11. 校验与回归

自动校验：

```powershell
npm.cmd run validate
```

当前会检查：

- `package.json` 和 `package-lock.json` 可解析。
- `app.js`、Electron main/preload 语法。
- HTML ID 和函数声明不重复。
- 必要控件和项目文档存在。
- 三处版本号一致。
- 损坏缓存备份逻辑存在。
- 重复分类/人物 ID 可以恢复。
- 电商、出行、学习三类 AI 日程模拟。
- 稀疏 AI 排期能够补成完整每日计划。
- 源码中没有明显 API Key。

人工回归按 `TEST_CHECKLIST.md` 执行，重点检查：

- 人际网切换动画和返回动画。
- 分类、人物、记忆增删改查。
- 对方开场、我方开场、输入中气泡、选项发送和真实记忆保存。
- 文件导入预览、确认和蒸馏。
- 日程生成、同步、防重复和延期。
- 数据导出、加密导出、导入恢复。
- 日间/夜间和最小窗口布局。

## 12. 已知问题与风险

1. `app.js` 体积较大，长期应按状态、AI、人物、对话、日程和数据迁移逐步拆分，但不要为了拆分而一次性大改。
2. API Key 仍由桌面前端直接调用模型，只适合本地个人使用。
3. 微信没有通用官方明文导出，需要继续研究合法、稳定、尊重隐私的导入方案。
4. QQ、钉钉、飞书格式适配仍不够稳定，需要真实样本验证。
5. 当前没有账号、数据库、云同步、冲突解决和多设备加密。
6. 自动更新只有客户端入口，没有公开更新服务器。
7. 构建仍使用 Electron 默认图标，需要补正式应用图标、安装包品牌素材和代码签名。
8. 目前自动测试偏状态和结构校验，完整 UI 流程仍主要依赖人工或 Playwright/CDP 回归。
9. `release/` 不在 GitHub 中；换机后需要重新构建或单独复制安装包。

## 13. 下一阶段优先级

### 第一优先级：真实使用稳定性

- 连续真实使用 1-2 周，记录最常见卡点。
- 修复聊天流程、记忆保存、人物画像和日程中的真实问题。
- 为关键流程补更稳定的端到端测试。

### 第二优先级：聊天导入

- 收集微信、QQ、钉钉、飞书真实可读导出样本。
- 为每种格式做独立解析适配和说话人识别。
- 大文件使用文件预览、分段蒸馏、进度与错误报告，避免粘贴几千条文本。

### 第三优先级：后端模型代理

- 桌面端不再直接保存公开服务的共享 Key。
- 后端支持 DeepSeek/OpenAI 兼容模型、用户额度、限流、日志和成本控制。
- 保留用户自带 Key 和完全离线模式作为选项。

### 第四优先级：账号与云同步

- 登录、设备管理、端到端加密或服务端加密。
- 人物、记忆、日程、模型偏好同步。
- 离线修改、版本号、冲突检测和手动选择。

### 第五优先级：自动更新与正式分发

- 配置更新域名和 HTTPS。
- 上传 `latest.yml`、安装包和 blockmap。
- 增加应用图标、代码签名、隐私政策和崩溃日志方案。

### 长期规划

- PC 聊天软件旁悬浮回复建议和对方回复预测。
- 联系人与本人人际网人物匹配。
- 手机端实时辅助。
- 声纹识别、对话记录和事后复盘。

这些长期功能涉及聊天监控、麦克风、隐私和平台权限，正式上线服务器时必须重新提醒并单独做授权、隐私和安全设计，当前不要提前接入。

## 14. 换机完成后的验收清单

文档编写时已确认旧电脑的 `%APPDATA%\ai-relationship-butler` 数据目录存在，`release\AI人际管家-Setup-0.2.0.exe` 安装包也存在。它们都不在 GitHub 中，换机前仍需由用户单独备份。

- [ ] 新电脑可以访问 GitHub 仓库。
- [ ] `npm.cmd ci` 成功。
- [ ] `npm.cmd run validate` 全部通过。
- [ ] `npm.cmd run desktop` 正常打开，无白屏。
- [ ] 人物、分类、记忆、真实聊天和日程已经导入。
- [ ] 人物数量和旧电脑一致。
- [ ] DeepSeek/API Key 已重新配置且连接测试成功。
- [ ] 关闭重开软件后数据仍在。
- [ ] 日间/夜间模式和最小窗口正常。
- [ ] 可以生成一次回复建议和一次 AI 日程计划。
- [ ] 可以完成一次 `npm.cmd run dist:win`。
- [ ] 新电脑 Git 提交和推送正常。

完成以上项目后，即可认为项目已在新电脑上完整接续。

## 15. 可直接交给新电脑 Codex 的接续提示词

在新电脑克隆仓库后，可以把下面这段话直接发给 Codex：

```text
请先完整阅读仓库根目录的 PROJECT_HANDOFF.md、README.md、PROJECT_PROGRESS.md、DESKTOP_APP.md 和 TEST_CHECKLIST.md，再检查 git status、package.json、app.js、index.html、styles.css、electron/ 和 scripts/validate.js。

这是 AI人际管家 Windows Electron 桌面项目，软件版是唯一真实主线，不要重新建立独立网页版。当前版本和最新进度以 GitHub main 分支为准。真实人物、聊天记录和 API Key 不在仓库中，不要创建或提交真实数据。

继续开发时沿用现有状态结构和界面风格。任何有实际影响的软件更新都要同步三处版本号，运行 npm.cmd run validate，实际启动桌面程序检查，构建 Windows 安装包，整理 release 目录，然后提交并推送 GitHub。先告诉我你从交接文档理解到的当前阶段、最优先的三个下一步和本机环境检查结果，再开始修改。
```
